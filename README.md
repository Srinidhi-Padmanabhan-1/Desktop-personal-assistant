# Desktop-personal-assistant
Used to automate day to day tasks with your computer using voice commands
use command 'pip install -r requirements.txt' 
(Note - automation features using voice commands will be added soon, for that you need to install more libraries in the future)
run the file start_program.py to start the program
